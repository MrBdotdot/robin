# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build output
dist
dist-ssr
*.local

# tsc -b incremental cache (regenerated on every typecheck)
*.tsbuildinfo

# Claude Code session state
.claude/

# Dependencies
node_modules

# Env files (keep .env.example tracked, ignore everything else)
.env
.env.local
.env.*.local

.vercel

# Supabase CLI local state (per-machine, not for VCS)
supabase/.temp/
supabase/.branches/
